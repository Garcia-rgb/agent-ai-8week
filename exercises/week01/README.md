# 第 1 周闭卷练习

先不要看 `task_cli.py`，自己实现以下需求：

- `add <title>`：创建任务
- `list`：列出任务
- `done <id>`：标记完成
- `delete <id>`：删除任务
- 数据保存在 UTF-8 JSON 文件
- 文件不存在、ID 不存在和 JSON 损坏时给出清晰错误

完成后再与参考实现对照。第二天删除自己写的版本，45 分钟内重新完成核心逻辑。

