# 学习面试题与参考答案

> 开始记录：2026-09-14
> 用途：按正式学习章节沉淀面试题和标准答案。当前从第 3 周 Day 4 开始记录，后续每完成一节持续追加。

## 使用方式

每节课结束时固定完成两道面试级问题。问题应要求解释原理、设计取舍和项目实现，答案所需知识必须已经包含在本节教学中。记录分为：

1. 面试问题。
2. 标准答案。
3. 30 秒面试表达。
4. 当前项目对应位置与后续改进。

复习时先只看问题并口述，再对照参考答案。参考答案用于整理表达，不代表已经能够闭卷编码。

---

## 第 3 周 Day 4：LLM 超时、重试、错误映射与降级

### 面试题 1：如何为 LLM API 设计错误分类与重试策略？

#### 标准答案

重试策略应根据错误是否可能自行恢复来决定：

- `400` 请求格式错误、`401` 密钥或权限错误属于确定性问题。在配置或代码不改变时，重试没有意义，应立即失败并报告配置或请求错误。
- `429` 限流、`500/503` 服务异常和连接或读取超时可能是暂时故障，可以结合服务端建议等待时间或指数退避进行有限重试。
- 响应 JSON 或字段结构损坏属于协议异常。可以在严格次数限制下再次尝试，但不能无条件重复。

不能无限重试，因为这会增加费用和延迟、占用连接与计算资源、加重故障服务压力，并可能在写操作中造成重复执行。生产实现还应设置最大尝试次数、总时间上限和退避等待。

职责应分层：

```text
底层 LLM 客户端
→ 设置超时
→ 分类 HTTP 与网络异常
→ 执行有限重试
→ 将厂商异常映射为项目异常
→ 在内部日志保留真实原因

上层 Agent
→ 决定返回降级答案、原始检索资料、稍后重试或转人工
→ 保存会话和业务状态
→ 不向用户泄露密钥、堆栈和内部请求
```

#### 30 秒面试表达

> 我不会对 LLM API 的所有异常统一重试，而是区分确定性错误和暂时性错误。`400/401` 需要修复请求或配置，不重试；`429`、`503` 和网络超时可以指数退避并限制次数。底层适配器负责超时、重试和错误映射，上层 Agent 负责业务降级。这样可以避免费用和延迟失控，也能防止写操作重复执行。

### 面试题 2：LLM 失败后返回 `200` 降级响应有什么利弊，如何改进？

#### 标准答案

捕获模型异常并返回友好结果，可以保证客服主接口仍然可用，保存用户问题与会话，并允许用户稍后重试或转人工。如果降级结果本身仍有业务价值，返回 `200` 是可以接受的。

问题在于仅返回一段自然语言时，前端和监控系统无法可靠区分正常模型答案与降级答案，可能把模型故障错误地统计为成功。应增加结构化状态，例如：

```json
{
  "answer": "模型暂时不可用，下面是检索到的原始资料。",
  "response_status": "degraded",
  "answer_source": "retrieval_only",
  "retryable": true,
  "citations": []
}
```

用户侧只显示安全、友好的信息；内部日志和指标记录具体状态码、异常类型、重试次数、模型名称、耗时和 `request_id`。如果系统已经提供了可用降级答案，可以返回 `200` 并明确标注降级状态；如果完全无法提供有效结果，更适合返回 `503 Service Unavailable`。

#### 30 秒面试表达

> LLM 故障时返回降级答案可以保护用户体验和会话状态，但如果仍返回普通 `200` 且没有结构化标记，前端和监控会误判为正常成功。我会在响应中加入 `degraded`、答案来源和是否可重试等字段，同时在内部记录真实异常。如果能返回有用的检索资料可保留 `200`；完全无结果时返回 `503`。

### 当前项目对应情况

- `src/support_agent/services/llm.py`：保留 30 秒超时；已实现最多三次尝试、1 秒和 2 秒退避等待、HTTP/网络/响应结构错误分类，以及带 `category` 和 `retryable` 的统一 `LLMError`。
- `401/403` 和其他非暂时性请求错误不重试；`429/500/502/503/504`、超时和网络异常进行有限重试。
- `src/support_agent/services/agent.py`：已捕获 `LLMError`，保存会话并返回友好提示。
- `tests/test_llm_retry.py`：使用 Mock 验证 `401` 不重试、`429/503` 尝试三次、超时后可恢复，以及响应结构损坏不重试；测试不访问真实模型。
- 全量测试为 `28 passed`，Ruff 检查通过。
- 后续改进：降级结果还没有结构化状态字段，日志也没有单独记录模型失败原因；当前退避尚未读取服务端等待提示，也未加入随机抖动和总时间预算。

---

## 第 3 周附加节：SmartPV 知识库导入与检索隔离（2026-09-15）

### 面试题 1：`corpus_id` 过滤和最低相关度阈值分别解决什么问题？为什么生产级 RAG 需要同时具备两者？

#### 标准答案

两者作用在不同层面，覆盖的是不同的失败模式：

- `corpus_id` 过滤解决**范围问题**：它决定这次检索允许看哪些资料。库里同时存在演示文档、正式手册和其他业务语料时，如果不过滤，混合检索会跨语料比较分数，让不相干的语料挤进 Top-K。按 `corpus_id` 限定后，检索只会落在目标知识库内。
- 最低相关度阈值解决**质量问题**：它决定范围内的结果里哪些够格被使用。即使范围正确，用户问的问题也可能在该知识库里根本没有对应内容；此时检索仍会返回分数最高的若干片段，只是分数整体偏低。阈值把「低分但仍排第一」的片段拦掉，让系统有机会拒答。

只留其中一个都会出问题：

- 只有阈值、没有 `corpus_id`：不同语料的分数分布不同，一个语料里的低分片段可能因为词面巧合越过阈值，反而挤掉真正该命中的内容；阈值也因此失去可比性。
- 只有 `corpus_id`、没有阈值：范围内必然返回 Top-K，哪怕 Top-1 只有 0.05 分，系统也会把这段几乎无关的内容交给模型，模型很可能据此编出一个看起来合理的答案。

所以生产级 RAG 需要「先缩小范围，再判断可靠性」，两者是串联关系：`corpus_id` 决定候选集，`min_score` 决定候选集里哪些可用，候选集为空或全部低于阈值时走拒答。

#### 30 秒面试表达

> `corpus_id` 解决的是检索范围，避免不同知识库的语料互相污染排名；最低相关度阈值解决的是检索质量，避免知识库里根本没有答案时，硬把分数最高的无关片段喂给模型。两者缺一不可：没有范围限制，阈值在不同语料的分数分布下失去可比性；没有阈值，范围内永远会返回 Top-K，系统就没有拒答能力。我目前的实现是先按 `corpus_id` 过滤，再按 `min_score` 卡分，命中为空时直接拒答、不调用模型。

### 面试题 2：为什么不能把 `0.4` 当成适用于所有知识库和 Embedding 模型的固定标准？阈值过高或过低分别会发生什么？

#### 标准答案

阈值是一个**相对量**，它的合理取值取决于打分函数的定义和实际分数分布，不是可以跨系统复制的常数：

- 打分函数的量纲不同。当前实现是 `0.55 * 词面重叠 + 0.45 * 余弦相似度`，两项都归一到 0～1，所以 0.4 在这套函数下有具体含义。换成纯向量检索、BM25 或带 rerank 的两阶段方案，分数尺度和分布完全变了，同一个数字不再等价。
- Embedding 模型换了，相似度分布就变了。本项目当前用的是本地 Hash Embedding（384 维），它只能做词面近似，语义无关的两段文本也可能拿到不低的余弦值；换成真正的语义模型后，无关文本的基线通常更低、匹配文本更高，阈值必须重新标定。
- 语料本身影响分布：分卷手册、聊天记录、法规条文的词面重复度和句子长度差别很大，同一阈值在不同语料上的误拒与误放比例相差明显。
- 切块方式也影响：`chunk_size` 越大，单块里出现的词越多，词面命中的机会越大，整体分数会被抬高。

阈值过高和过低的表现正好相反：

- **过高**：把本来正确的片段挡在门外，命中为空的比例上升，系统频繁拒答或转人工，用户会觉得「手册里明明有，它却说不知道」，属于召回率下降。
- **过低**：无关片段也能过线，模型拿到一堆低质量资料，容易生成看似有理但没有依据的答案，拒答形同虚设，属于准确率下降、幻觉风险上升。

正确做法是用**评测集标定**：准备一批「知识库里确实有答案」和「知识库里确实没有」的两类问题，扫描候选阈值，观察通过率和误答率，选一个在当前语料和当前 Embedding 下平衡点最合适的值，把它做成配置项（本项目即 `RETRIEVAL_MIN_SCORE`）而不是代码里的硬编码常数；Embedding 模型或切块策略变更后必须重新标定。

#### 30 秒面试表达

> 因为阈值是相对打分函数和分数分布定义的，不是绝对常数。当前 0.4 对应的是「词面 0.55 + 余弦 0.45」这套打分，加上本地 Hash Embedding 和 700 字分块。换成别的 Embedding、别的检索算法或别的语料，分数分布就变了，同一个数字的含义也就变了。阈值过高会把正确片段挡掉、召回下降、系统频繁拒答；过低会让无关片段过线、模型拿到噪声、幻觉变多。所以我会用评测集扫一遍候选阈值，选好平衡点后放进配置项，模型或切块策略一变就重新标定。

### 当前项目对应情况

- `src/support_agent/services/rag.py`：`RAGService.search()` 新增 `corpus_id`、`include_restricted`、`min_score` 三个参数；先按 `chunk_metadata["corpus_id"]` 过滤范围，再按 `score >= min_score` 判断是否命中。
- `src/support_agent/services/knowledge_base.py`：导入时为每个章节写入 `corpus_id=smartpv_v2`、`visibility=local_only`、`restricted` 等元数据，并默认排除账号密码、密码重置章节。
- `src/support_agent/config.py`：`retrieval_corpus_id`（默认 `None`）和 `retrieval_min_score`（默认 `0.0`）作为可配置项，不在代码里写死。
- `docker-compose.yml`：API 服务通过环境变量注入 `RETRIEVAL_CORPUS_ID=smartpv_v2` 与 `RETRIEVAL_MIN_SCORE=0.4`；PostgreSQL 端口改为 `127.0.0.1:5433:5432`，只绑本机回环。
- `src/support_agent/services/agent.py`：检索命中为空时不调用模型，直接返回「当前知识库没有找到足够可靠的依据」。
- `tests/test_knowledge_base.py`：6 个测试覆盖章节切分、受限章节默认排除与显式包含、分卷缺失报错、导入后元数据落库，以及 `corpus_id` 隔离（用 `missing_corpus` 查询返回空）。
- 全量测试 `34 passed`，Ruff 通过。
- 后续改进：`0.4` 目前是手工设定的经验值，还没有用评测集正式标定；也尚未实现按用户或租户动态切换 `corpus_id`，以及受限章节的独立授权访问通道。

## 第 3 周 Day 5：手写 Agent Loop 与工具调用边界（2026-09-15）

### 面试题 1：模型返回的工具名和参数为什么不能直接执行？服务端需要做哪些校验？

#### 标准答案

因为模型输出和用户输入属于同一类东西：**都是外部不可信输入**。它只是「申请调用某个工具」，不是「已经通过授权的操作」。把模型的输出当成命令直接执行，等于把模型当成了权限系统。

要分清两份东西：

- **工具说明书**：随请求发给模型的 `tools` 字段，只包含名字、用途和参数结构，作用是让模型知道有哪些能力可用。
- **工具白名单**：服务端进程内的工具注册表，才是真正决定「哪个函数可以被执行」的地方。

模型可能编出一个不存在的工具名，也可能给一个语义正确但格式错误的参数，甚至在提示词注入下试图调用越权工具——这些在说明书层面都拦不住，只有服务端能拦。

服务端至少要过这几层校验：

1. **工具名白名单**：不在注册表里的名字一律拒绝并记录。不要用动态查找、`getattr`、反射或字符串拼名字去执行函数。
2. **参数格式**：模型给的是 JSON 字符串，先解析，解析失败就回错；还要检查结果确实是 JSON 对象，而不是数组或标量。
3. **必填与类型**：用 JSON Schema 或 Pydantic 校验必填字段和字段类型，别指望模型每次都传对。
4. **额外字段**：出现的未定义字段要**拒绝**而不是静默忽略。静默忽略会让越权参数悄悄通过，一旦以后实现改成读取整个参数字典，漏洞就暴露了。
5. **业务与权限校验**：即使参数格式完全合法，也要校验这条数据是否属于当前用户、这个操作是否有权限、金额和范围是否在允许区间。
6. **写操作人工确认**：创建工单、退款、改配置这类写操作不能由循环自动执行，必须走确认令牌或人工审批。
7. **执行层兜底**：工具自身还要有边界，例如本项目计算器用的是白名单 AST 而不是 `eval()`，从根上排除执行任意代码的可能。

一句话概括：**模型负责提议，服务端负责授权和执行**。提示词可以被绕过，检索过滤可以被污染，但工具执行层是最后一道防线，必须假设上游全部不可信。

#### 30 秒面试表达

> 因为模型输出属于不可信输入，它只是申请调用工具，不是授权。我会把「工具说明书」和「工具白名单」分开：说明书发给模型，白名单留在服务端。服务端要做白名单校验、JSON 解析、必填和类型校验、拒绝未定义字段，再做业务权限校验；写操作必须人工确认，工具本身也要有边界，比如计算器用白名单 AST 而不是 eval。核心是一句话：模型负责提议，服务端负责授权和执行。

### 面试题 2：Agent Loop 为什么必须设置最大轮数？轮数用尽后应该怎么处理？

#### 标准答案

因为循环的终止条件掌握在模型手里，而模型不保证收敛。

具体风险有几类：

- **死循环**：模型调用工具 → 对结果不满意 → 换个参数再调 → 仍然不满意，一直重复。
- **错误重试成瘾**：工具持续返回失败，模型把失败理解成「需要再试一次」，于是一直用同一路径重试。
- **成本与延迟失控**：每一轮都是一次完整的模型调用，输入还在不断变长（历史消息越堆越多），一次用户提问可能被放大成几十次调用和成倍的费用。
- **故障放大**：如果某个工具有副作用，不受限的重试可能造成重复写入。

所以最大轮数不是「优化」，是**安全阀**。它和 API 网关的超时、重试上限属于同一类设计：**任何可能不受控的循环都必须有硬上限**。

轮数用尽后的处理方式，原则是「降级收场，而不是抛异常」：

1. **禁用工具再问一次**。最后一轮把 `tools` 从请求里去掉，模型没有可调用的工具，只能用手上已有的信息组织答案。这样用户至少能拿到一个基于部分信息的回答。
2. **明确告知用户信息不完整**。回答里应说明没有拿到完整依据，并给出下一步（补充信息、转人工）。
3. **保留执行轨迹**。把每一轮调用的工具名、参数、成功与否、输出记下来，用于事后排查和统计——比如「工具失败率」「平均轮数」都是很有价值的指标。
4. **区分停止原因**。是模型正常给出答案（`final_answer`）、轮数用尽（`max_rounds`），还是模型故障（`llm_error`）？这三种情况在监控和告警里含义完全不同，不能只返回一个字符串。
5. **轮数上限要可配且可观测**。上限设得太小会让复杂问题被截断，设得太大则失去保护意义；平均值接近上限说明工具设计或提示词有问题，应该去优化工具粒度，而不是简单调大数字。

另外要注意：限制轮数是**兜底**，不是**目标**。如果发现模型经常触顶，正确做法是改进工具描述、把粗粒度工具拆成更明确的工具、或让工具返回更可用的错误信息，让模型一次就能拿到需要的资料。

#### 30 秒面试表达

> 因为终止条件在模型手里，模型不保证收敛：可能反复换参数重试、可能对工具失败一直重试，而每一轮都是完整的模型调用，输入还在变长，成本和延迟会被放大。所以最大轮数是安全阀，和 API 网关的超时、重试上限是同一类设计。用尽之后我不会抛异常，而是禁用工具再问一次，让模型用手上已有的信息作答，回答里说明信息不完整，同时保留每轮的调用轨迹和停止原因，用于监控和排查。如果模型经常触顶，应该回去优化工具粒度，而不是把数字调大。

### 当前项目对应情况

- `src/support_agent/services/agent_loop.py`（新增）：`MAX_ROUNDS = 5`；`build_tool_registry()` 是服务端唯一可信的工具执行表；`ToolSpec.as_schema()` 只输出 `name`/`description`/`parameters`，`handler` 绝不随请求发给模型。
- 参数校验四道关：`parse_arguments()` 依次检查「能解析成 JSON」「结果必须是 JSON 对象」「不能出现未定义参数」「必填字段齐全且类型正确」，失败抛 `ToolArgumentError`。
- `execute_tool_call()` 返回 `ToolOutcome(ok, text)`，未知工具、写操作、参数错误、业务错误、工具自身异常全部转成可回传文本，**永不向上抛异常**。
- `ToolSpec.writes` 标记写操作，`execute_tool_call()` 直接拒绝，不调用 handler；与项目原有「创建工单需要确认令牌」的设计一致。
- 轮数用尽后调用 `model.chat_with_tools(messages, None)`，请求里不带 `tools` 字段，强制模型收敛；`LoopResult.stopped_reason` 区分 `final_answer` / `max_rounds` / `llm_error:<category>`。
- 工具执行轨迹记在 `ToolCallRecord`（轮次、工具名、原始参数、是否成功、输出），可用于回放和统计。
- `src/support_agent/services/llm.py`（改写）：抽出 `_chat()` 统一处理超时、有限重试与错误分类，`answer()` 与 `chat_with_tools()` 共用；新增 `ToolCallRequest`、`AssistantTurn` 及严格解析的 `AssistantTurn.from_message()`。
- `tests/test_agent_loop.py`（新增 12 个测试）：覆盖直接作答、工具结果回传（含 `role=tool` 与 `tool_call_id`）、说明书字段边界、未知工具、非法 JSON、缺必填、多未定义字段、订单不存在、计算器沙箱不可绕过（`__import__("os").getcwd()`）、写操作不自动执行、5 轮触顶后禁用工具、模型故障降级。全部不连真实模型。
- `examples/agent_loop_demo.py`（新增）：用脚本化假模型跑通「计算器 + 订单查询」两轮调用并打印轨迹，无需 API Key。
- 全量测试 `46 passed`，Ruff 通过。
- 后续改进（Day 6 已部分完成）：会话历史已回填进 Agent Loop、循环已接入 `POST /chat`、写操作已接上人工确认令牌；仍未做的是参数类型校验只覆盖 `string`/`number`、未知工具与工具失败率没有独立指标。

---

## 第 3 周 Day 6：将 Agent Loop 接入 `POST /chat`（2026-09-15）

### 面试题 1：提示词注入的检测为什么必须放在 Agent Loop 之外？把它做成一个工具、或者写进系统提示词里，会有什么问题？

**问题拆解**

这道题问的不是「怎么检测注入」，而是「这道防线应该放在架构的哪一层」。面试官想听的是你对**信任边界**的判断。

**参考回答**

核心判断是：**被保护的对象不能同时是保护者**。

Agent Loop 里的「模型」是一个概率系统，而且它的输入里就包含用户输入。攻击者做提示词注入的目的，恰恰是让模型**相信**某些本该被拒绝的事是被允许的。所以如果把安全检查交给模型判断，就出现了一个循环依赖：防线能不能生效，取决于攻击对象有没有被说服。

具体到两种错误做法：

- **做成一个工具**（比如 `check_injection(text)`）：等于把「要不要检查」的决定权交给模型。模型被说服时，它根本不会去调这个工具，检查永远不会发生。工具做的是「执行动作」，不是「约束决策」。
- **写进系统提示词**（比如「你要拒绝绕过系统规则的请求」）：提示词是**建议**，不是**约束**。它在正常输入下大概率有效，在对抗输入下恰恰是最先失效的东西。用它对正常用户做安全，用它对攻击者做安全，是两件不同的事。

正确做法是在模型之前用确定性代码拦：用户消息进来 → 规则/分类器判断 → 命中就返回固定话术并记审计，**根本不调用模型**。这样即使模型在别处被成功注入，这条路径也不可达。

**代价与局限（要主动说，否则答案不合格）**

- 关键词匹配会误伤：用户正常问「system prompt 是什么意思」「怎么防止忽略之前指令这类攻击」都可能被误判。生产环境需要分级——第一道确定性规则紧一点，命中后不一定直接拒绝，也可以降级、加人工、或进二次判定。
- 只靠关键词一定不够，需要规则 + 分类模型 + 输出侧过滤三者配合。
- 但无论怎么分层，**最外层那道必须是不依赖模型的**。这是这道题要的结论。

**30 秒面试表达**

> 因为被保护的对象不能同时是保护者。模型是概率系统，而它的输入里就包含攻击者的输入，注入的目的就是让模型相信这件事是允许的。做成工具，等于把「要不要检查」交给模型决定；写进提示词，等于把硬约束降级成建议。所以我的做法是在进循环之前用确定性代码拦，命中就返回固定话术并写审计日志，模型一次都不调用——测试里直接断言这种情况下模型调用次数为零。

---

### 面试题 2：本地规则模型完全不做语言理解，答案还是模板拼的。为什么还要写它？它的价值是什么？

**问题拆解**

面试官在问：你加了一个「假的模型」，是权宜之计还是有工程价值？这题考察的是**可测性与接口抽象的取舍**，不是模型能力。

**参考回答**

先说它不是的东西：它不是产品功能，也不是用来「省一个 API Key」。它是一条**让真实链路在没有外部依赖时也能被完整验证**的通道。

三个具体价值：

1. **测试能断言真实行为，而不是断言 mock。** 没有它，所有 `/chat` 测试都只能 mock 掉模型、再断言「mock 被调用了」。而真正要守的东西——工具白名单生效了没有、参数校验拦住了没有、写操作是不是真的没执行、检索为空时答案有没有被覆盖——全都在模型之后的路径上。本地模型让这些断言可以在 CI 里真跑，不需要密钥、不需要网络、结果还完全确定。
2. **它反过来检验了抽象是否成立。** Agent Loop 只依赖一个 `ChatModel` 协议。如果换一个完全不同的实现还能跑通，说明循环的耦合确实只在协议上——它在循环里不是「模型」，只是一个「会提出工具申请的东西」。
3. **降级与离线演示。** 配额用尽、内网环境、演示机器上没配密钥时，功能仍然在（弱化版），而不是整站挂掉。

**代价与风险（必须主动说）**

- 它只覆盖规则能表达的意图，规则之外的问题它接不住。
- 它的答案质量是模板拼装的，绝不能拿来当「AI 效果」汇报。文档里必须写清它是降级设施，不是能力。
- 它固定两轮（申请工具 → 收敛），因为它没有能力在一轮里既申请工具又给结论——这个差异在测轮数相关的逻辑时要意识到。

**30 秒面试表达**

> 它存在的价值不是省密钥，而是让整条链路可测。因为真正要守的逻辑——工具白名单、参数校验、写操作拦截、空检索覆盖答案——全都在模型调用之后，如果只能 mock 模型，这些就只能靠断言 mock 间接验证。有了本地规则模型，CI 里能真跑完整路径，结果还是确定的。另外它也是对我抽象的一次检验：循环只依赖一个返回工具申请的接口，换一个完全不同的实现还能跑通，说明耦合是干净的。当然它只是降级设施，答案质量不能当能力汇报。

---

### 当前项目对应情况

- `src/support_agent/services/agent.py`：`respond()` 里 `looks_like_prompt_injection(text)` 在 `run_agent_loop()` **之前**执行，命中即返回固定话术并写 `AuditLog(action="prompt_injection_blocked")`。测试 `test_prompt_injection_is_blocked_before_the_loop` 断言 `model.calls == []`——模型一次都没被调用。
- `src/support_agent/services/local_model.py`（新增）：`RuleBasedLocalModel` 实现与 `OpenAICompatibleClient` 相同的 `chat_with_tools(messages, tools)`，复用 `graph.py` 的规则分类把意图映射成工具申请，再用模板把工具结果拼成回答。`llm_enabled` 为假时由 `SupportAgent` 自动选用。
- `src/support_agent/services/agent_loop.py`：`ToolSpec.handler` 支持协程；`execute_tool_call()` 改异步并用 `inspect.isawaitable()` 兼容两种工具；`build_support_registry()` 把需要请求上下文的 `search_knowledge_base` 与 `create_ticket` 挂进同一张白名单。
- 空检索兜底：`respond()` 中若本轮调用过 `search_knowledge_base` 但去重后没有任何命中，直接覆盖成拒答话术。测试 `test_empty_retrieval_overrides_the_model_answer` 用一个会编造答案的假模型验证覆盖生效。
- 写操作：`ToolOutcome` / `ToolCallRecord` 带 `requires_confirmation`，循环以 `stopped_reason="needs_confirmation"` 收场，`SupportAgent` 换成 `create_confirmation_token` 签发的确认令牌；参数校验在写操作判定**之前**执行。
- 会话历史：`run_agent_loop(history=...)` 把历史消息插在 system 之后、本轮消息之前；`_history()` 在本轮用户消息落库**之前**读取。工具轨迹写入 `AuditLog(action="agent_loop_tool_calls")`，含轮数、停止原因与每次调用的名字和成败。
- 配置：`agent_max_rounds`（默认 5）与 `chat_history_limit`（默认 10）。
- 测试：新增 24 个（`test_local_model.py` 9 个、`test_agent_integration.py` 12 个、`test_agent_loop.py` 3 个），全量 `46 passed` → `70 passed`，Ruff 通过。
- 后续改进：参数校验尚不支持枚举与嵌套对象；工具返回结果没有做注入过滤和长度截断；未知工具与工具失败率还没有独立指标；跨轮的轮数预算（一个会话内累计轮数上限）尚未实现。
